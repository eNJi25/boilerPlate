{% extends 'base.html.twig' %}

{% block title %}Sorties{% endblock %}

{% block body %}

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Nom</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>
        {% for sortie in sorties %}
        <tr>
            <td>{{sortie.id}}</td>
            <td>{{sortie.libelle}}</td>
            <td>
                <a href="{{path('sortie_show', {'id': sortie.id})}}" class="btn btn-primary">Détails</a>
                <a href="{{path('sortie_edit', {'id': sortie.id})}}" class="btn btn-primary">Modifier</a>
                <a href="{{path('sortie_delete', {'id': sortie.id})}}" class="btn btn-primary">Supprimer</a>
            </td>
        </tr>
        {% endfor %}

    </tbody>
</table>

<a href="{{path('sortie_create')}}" class="btn btn-primary">Ajouter une marque</a>

{% endblock %}