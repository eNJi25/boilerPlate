{% extends 'base.html.twig' %}

{% block title %}Adhesion{% endblock %}

{% block body %}

<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Année</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{adhesion.id}}</td>
            <td>{{adhesion.annee}}</td>
        </tr>
    </tbody>
</table>

<a href="{{path('adhesion_index')}}" class="btn btn-primary">Retour à la liste</a>

{% endblock %}